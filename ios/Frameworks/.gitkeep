# This directory is for the sherpa-onnx XCFramework
# 
# The framework can be:
# 1. Downloaded from GitHub Actions artifacts (recommended)
# 2. Built locally using build-ios.sh
# 3. Committed to the repository (if size allows)
#
# To download from GitHub Actions:
# 1. Go to https://github.com/XDcobra/react-native-sherpa-onnx-stt/actions
# 2. Find the latest "Build sherpa-onnx XCFramework" workflow run
# 3. Download the sherpa-onnx-xcframework-zip-* artifact
# 4. Extract it here: unzip sherpa-onnx-xcframework-zip-*.zip -d .
#
# The Podspec will automatically detect and use the framework if it exists here.
